# 🧪 دليل الاختبار - ميزة الصورة الشخصية

## 📋 معايير النجاح

| المعيار                | Admin | Resped | Directeur |
| ---------------------- | ----- | ------ | --------- |
| ✅ عرض قسم الصورة      | ✓     | ✓      | ✓         |
| ✅ عرض أيقونة الكاميرا | ✓     | ✓      | ✓         |
| ✅ تحميل الصورة        | ✓     | ✓      | ✓         |
| ✅ حذف الصورة          | ✓     | ✓      | ✓         |
| ✅ عرض الأحرف الأولى   | ✓     | ✓      | ✓         |
| ✅ رسائل الخطأ         | ✓     | ✓      | ✓         |

---

## 🚀 قبل البدء

### المتطلبات:

- [ ] Node.js مثبت
- [ ] الـ Backend مشغل وعلى port 5000
- [ ] الـ Frontend مشغل وعلى port 3000
- [ ] المجلد `./images/users/` موجود في الـ Backend
- [ ] متصفح حديث (Chrome/Firefox/Edge)

### الأدوات المطلوبة:

- [ ] صورة JPG/PNG بحجم < 1 MB للاختبار
- [ ] حساب Admin مع صلاحية التسجيل الدخول
- [ ] حساب Responsable Pédagogique
- [ ] حساب Directeur

---

## 🧑‍💻 خطوات التشغيل

### 1. تشغيل الـ Backend

```bash
cd c:\projects\Projet-Pfe\backend

# تثبيت المتطلبات (إذا لم تكن مثبتة)
npm install

# تشغيل الـ Backend
npm run start
```

**النتيجة المتوقعة:**

```
✓ Backend running on http://localhost:5000
✓ Database connected
✓ Server started
```

### 2. تشغيل الـ Frontend

في **نافذة Terminal جديدة:**

```bash
cd c:\projects\Projet-Pfe\frontend

# تثبيت المتطلبات (إذا لم تكن مثبتة)
npm install

# تشغيل الـ Frontend
npm run dev
```

**النتيجة المتوقعة:**

```
✓ Frontend running on http://localhost:3000
✓ Next.js compiled successfully
```

---

## ✅ الاختبار 1 - Admin Settings

### الدخول إلى الصفحة:

```
رابط: http://localhost:3000/admin/settings
```

### الخطوات:

#### 1. التحقق من ظهور القسم:

- [ ] ابحث عن "Photo de profil" في الصفحة
- [ ] يجب أن تظهر صورة دائرية (أو الأحرف الأولى)
- [ ] يجب أن تظهر أيقونة كاميرا بجانب الصورة

#### 2. اختبار التحميل:

- [ ] اضغط على أيقونة الكاميرا
- [ ] اختر صورة من جهازك
- [ ] انتظر لحظة قصيرة
- [ ] تحقق من أن الصورة ظهرت بدلاً من الأحرف
- [ ] افتح الـ console (F12) وتحقق من عدم وجود أخطاء

```javascript
// Console يجب أن تكون فارغة من الأخطاء
// في Network tab يجب أن ترى:
// POST /profile/upload-image - 200 OK
```

#### 3. اختبار الحذف:

- [ ] انتظر حتى تظهر الصورة بالكامل
- [ ] يجب أن تظهر أيقونة حذف (X) في الزاوية السفلية اليسرى
- [ ] اضغط على أيقونة الحذف
- [ ] اضغط OK في نافذة التأكيد
- [ ] تحقق من أن الصورة اختفت والأحرف ظهرت مجدداً

#### 4. اختبار رسائل الخطأ:

- [ ] حاول تحميل ملف ليس صورة (مثل PDF)
- [ ] يجب أن تظهر رسالة خطأ: "Erreur lors de l'upload"

---

## ✅ الاختبار 2 - Responsable Pédagogique Settings

### الدخول إلى الصفحة:

```
رابط: http://localhost:3000/respedagogique/settings
```

### الخطوات:

#### 1. التحقق من الشكل (يجب أن يكون نفس Admin):

- [ ] قسم "Photo de profil" موجود
- [ ] أيقونة كاميرا موجودة
- [ ] النمط والألوان متطابقة

#### 2. اختبار التحميل:

- [ ] كرر نفس خطوات اختبار Admin
- [ ] يجب أن تعمل بنفس الطريقة

#### 3. اختبار الحذف:

- [ ] اضغط على أيقونة الحذف
- [ ] تأكد من أن الصورة تختفي

---

## ✅ الاختبار 3 - Directeur Settings

### الدخول إلى الصفحة:

```
رابط: http://localhost:3000/directeur/settings
```

### الخطوات:

#### 1. التحقق من ظهور القسم:

- [ ] قسم "Photo de profil" موجود
- [ ] الشكل يشبه Admin و Responsable

#### 2. اختبار التحميل:

- [ ] اضغط على الكاميرا
- [ ] حمّل صورة
- [ ] تحقق من أن الصورة تظهر فوراً (cache-busting يعمل)

#### 3. اختبار Cache-Busting:

- [ ] حمّل صورة أولى
- [ ] حمّل صورة ثانية مختلفة
- [ ] تحقق من أن الصورة الثانية تظهر وليس الأولى (بدون refresh)

#### 4. اختبار الحذف:

- [ ] احذف الصورة الحالية
- [ ] تحقق من أن الأحرف الأولى تظهر مجدداً

---

## 🔧 استكشاف الأخطاء

### الخطأ: لا تظهر أيقونة الكاميرا

**الحل:**

```bash
# تحقق من أن lucide-react مثبت
npm install lucide-react

# أعد تشغيل الـ Frontend
npm run dev
```

### الخطأ: الصورة لا تظهر بعد التحميل

**الحل:**

1. افتح Developer Tools (F12)
2. انظر إلى tab Network
3. تحقق من أن الطلب `POST /profile/upload-image` يرجع 200
4. إذا كان يرجع 401، السبب: token غير صحيح
5. إذا كان يرجع 400، السبب: ملف غير صحيح

### الخطأ: "Erreur lors de l'upload"

**الأسباب المحتملة:**

1. **الـ Backend مش مشغل:**

   ```bash
   # تأكد من تشغيل الـ Backend
   cd backend && npm run start
   ```

2. **الـ Token غير صحيح:**
   - افتح Console (F12)
   - اكتب: `localStorage.getItem('access_token')`
   - يجب أن تظهر قيمة token طويلة

3. **المجلد غير موجود:**

   ```bash
   # في الـ Backend
   mkdir -p ./images/users
   ```

4. **الملف ليس صورة:**
   - تأكد من أنك تحمل ملف صورة فقط
   - الأنواع المدعومة: JPG, PNG, GIF, WebP
   - الحد الأقصى: 1 MB

### الخطأ: لا يمكن حذف الصورة

**الحل:**

1. تحقق من الـ Console للأخطاء
2. تأكد من أن الصورة موجودة فعلاً في الـ Backend
3. تحقق من صلاحيات الـ Backend للحذف

---

## 📊 الاختبار الشامل - Checklist

### Admin:

- [ ] ظهور قسم الصورة
- [ ] ظهور أيقونة الكاميرا
- [ ] عمل تحميل الصورة
- [ ] ظهور الصورة فوراً
- [ ] ظهور أيقونة الحذف
- [ ] عمل الحذف
- [ ] عدم وجود أخطاء Console

### Responsable Pédagogique:

- [ ] ظهور قسم الصورة
- [ ] ظهور أيقونة الكاميرا
- [ ] عمل تحميل الصورة
- [ ] ظهور الصورة فوراً
- [ ] ظهور أيقونة الحذف
- [ ] عمل الحذف
- [ ] عدم وجود أخطاء Console

### Directeur:

- [ ] ظهور قسم الصورة
- [ ] ظهور أيقونة الكاميرا
- [ ] عمل تحميل الصورة
- [ ] ظهور الصورة فوراً
- [ ] عمل Cache-Busting (تحديث صحيح للصورة)
- [ ] ظهور أيقونة الحذف
- [ ] عمل الحذف
- [ ] عدم وجود أخطاء Console

---

## 🖼️ اختبار الحالات الحدودية

### 1. تحميل صورة كبيرة جداً:

```bash
# إنشاء ملف 2 MB
dd if=/dev/zero of=large.jpg bs=1M count=2

# حاول تحميله
# يجب أن تظهر رسالة خطأ (الحد الأقصى: 1 MB)
```

### 2. تحميل ملف ليس صورة:

- [ ] جرب تحميل ملف TXT
- [ ] جرب تحميل ملف PDF
- [ ] يجب أن تظهر رسالة خطأ

### 3. تحميل صورة مرتين:

- [ ] حمّل صورة أولى
- [ ] حمّل صورة ثانية
- [ ] يجب أن تحل الثانية الأولى

### 4. الحذف مرتين:

- [ ] حمّل صورة
- [ ] احذفها
- [ ] احذفها مرة أخرى
- [ ] يجب أن تظهر رسالة: "لا توجد صورة للحذف"

---

## 📝 تقرير الاختبار

### النموذج:

```
=== تقرير اختبار ميزة الصورة الشخصية ===

تاريخ الاختبار: [yyyy-mm-dd]
المختبر: [الاسم]

--- Admin ---
✓ ظهور القسم: نعم/لا
✓ التحميل: نعم/لا
✓ الحذف: نعم/لا
✓ الأخطاء: لا توجد/توجد

--- Responsable Pédagogique ---
✓ ظهور القسم: نعم/لا
✓ التحميل: نعم/لا
✓ الحذف: نعم/لا
✓ الأخطاء: لا توجد/توجد

--- Directeur ---
✓ ظهور القسم: نعم/لا
✓ التحميل: نعم/لا
✓ الحذف: نعم/لا
✓ Cache-Busting: نعم/لا
✓ الأخطاء: لا توجد/توجد

=== النتيجة النهائية ===
الحالة: ✅ نجح / ❌ فشل
الملاحظات: [أي ملاحظات]
```

---

## 🎯 معايير القبول

| المعيار                        | يجب أن يكون |
| ------------------------------ | ----------- |
| جميع الصور تظهر بشكل صحيح      | ✅ نعم      |
| جميع الأخطاء معالجة بشكل صحيح  | ✅ نعم      |
| لا توجد أخطاء TypeScript       | ✅ صحيح     |
| لا توجد أخطاء Console          | ✅ صحيح     |
| الوظائف تعمل على جميع الأدوار  | ✅ نعم      |
| الـ UI متطابق على جميع الأدوار | ✅ نعم      |

---

## ✨ ملاحظات مهمة

### أثناء الاختبار:

- افتح Developer Tools (F12) للتحقق من الأخطاء
- تحقق من Network tab لرؤية الطلبات
- تحقق من Console لأي رسائل خطأ

### بعد الاختبار:

- احفظ تقرير الاختبار
- وثّق أي مشاكل وجدتها
- شارك النتائج مع فريق التطوير

---

**تم إنشاء دليل الاختبار - جاهز للبدء! 🚀**
