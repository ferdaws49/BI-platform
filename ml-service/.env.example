# Example .env for ml-service
# Set your real DATABASE_URL before enabling USE_DB
DATABASE_URL=postgresql://user:password@host:5432/dbname
# Set USE_DB=1 to enable DB loaders in training scripts (default 0)
USE_DB=0
